runtime: go122
service: default

env_variables:
  FIRESTORE_PROJECT_ID: "esperanto-kurso"
  WEBAUTHN_RPID: "esperanto-kurso.net"
  WEBAUTHN_ORIGIN: "https://esperanto-kurso.net"

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto
